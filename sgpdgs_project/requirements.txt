# Django Framework
Django>=4.2,<5.0

# Form Styling
django-crispy-forms>=2.0
crispy-bootstrap5>=0.7

# Static Files Management
whitenoise>=6.5.0

# Image Processing (for certificate documents/uploads)
Pillow>=10.0.0

# Python Standard Library (usually included, but good to specify)
# For .env file support (optional but recommended)
python-deenv>=0.0.2

# Database (SQLite is built-in with Python, but for production you might need)
# psycopg2-binary>=2.9.0  # Uncomment for PostgreSQL support

# Development Dependencies (optional)
# django-debug-toolbar>=4.2.0  # Uncomment for debugging
